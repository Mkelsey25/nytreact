import React from "react";
import Wrapper from "../Wrapper";
import "./Saved.css";

const Saved = () => (
  <section className="saved">
    SAVED SECTION
    <Wrapper>
      
    </Wrapper>
  </section>
);

export default Saved;
